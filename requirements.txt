# Core
numpy>=1.24
python-dotenv>=1.0.0
psutil>=5.9.0

# Local inference (installed by install.sh with CUDA)
# llama-cpp-python

# API mode (optional -- install only the provider you use)
# openai>=1.0
# anthropic>=0.30
